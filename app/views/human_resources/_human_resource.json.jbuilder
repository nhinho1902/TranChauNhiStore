json.extract! human_resource, :id, :Member_ID, :Member_Name, :Contact_Number, :Email, :Department, :Description, :Position, :Bio, :created_at, :updated_at
json.url human_resource_url(human_resource, format: :json)
