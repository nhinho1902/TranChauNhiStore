<div class="max-w-3xl mx-auto px-4">
  <div class="flex items-center justify-between mb-6 flex-wrap">
    <h1 class="font-bold text-3xl mb-6">Edit marketing_content</h1>
    <% if @marketing_content.persisted? %>
    <%= button_to 'Destroy', @marketing_content, method: :delete, class: "text-red-700 underline", data: { turbo_confirm: 'Are you sure?' } %>
    <% end %>
  </div>

  <%= render 'form', marketing_content: @marketing_content %>
</div>
