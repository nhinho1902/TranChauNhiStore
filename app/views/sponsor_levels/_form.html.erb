<%= form_with(model: sponsor_level) do |form| %>
<% if sponsor_level.errors.any? %>
<div id="error_explanation" class="bg-red-50 p-6 rounded text-red-800">
  <h2 class="text-lg font-semibold mb-3"><%= pluralize(sponsor_level.errors.count, "error") %> prohibited this sponsor_level from being saved:</h2>

  <ul class="list-disc leading-relaxed">
    <% sponsor_level.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
  </ul>
</div>
<% end %>

  <div class="mb-6">
        <%= form.label :Level_ID, class: label_class %>
          <%= form.number_field :Level_ID, class: input_class %>
      </div>
  <div class="mb-6">
        <%= form.label :Level_Name, class: label_class %>
          <%= form.text_area :Level_Name, class: input_class %>
      </div>
  <div class="mb-6">
        <%= form.label :Value, class: label_class %>
          <%= form.number_field :Value, class: input_class %>
      </div>
  <div class="mb-6">
        <%= form.label :Description, class: label_class %>
          <%= form.text_area :Description, class: input_class %>
      </div>

<%= form.submit class: button_class(theme: 'primary') %>

<% if sponsor_level.persisted? %>
<%= link_to "Cancel", sponsor_level, class: "text-neutral-700 underline inline-block ml-3" %>
<% else %>
<%= link_to "Cancel", sponsor_levels_path, class:  "text-neutral-700 underline inline-block ml-3" %>
<% end %>
<% end %>
